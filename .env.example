SESSION_SECRET="replace-with-a-long-random-string"
APP_URL="http://localhost:3000"

# Optional — set to send real magic-link emails via SMTP.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="VespaDatabase <hello@vespadatabase.app>"
